# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/
/dist/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env*.local
.env

# typescript
*.tsbuildinfo
next-env.d.ts.bak

# ContentForest-specific
/_refs/
/_repos/
/content/_local/
/content/_test/
/validation-report.json

# Local-only planning / scratch agent docs
/agents/

# Nanocoder runtime state (local sessions, task list, checkpoints).
# We don't ship any project-level Nanocoder agents/commands/schedules in this
# repo — generation uses templated prompts under prompts/ instead — so the
# whole .nanocoder/ tree is runtime-only.
/.nanocoder/
